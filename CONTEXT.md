# CONTEXT.md

> **Cross-machine continuity bridge.** Read this first when resuming work on this project from any machine — it travels with the repo via git. Detailed conversation memory (feedback, decisions, deeper history) is mirrored at `Obsidian/myKnowledge/project-memory/PIDM-main/` in the shared Obsidian vault.

## What This Is
PIDM — Python Internet Download Manager fork: stream + file downloads, browser extension integration, multi-language UI.

## Stack
- Python, PySide6 (Qt), `yt_dlp` (stream downloads), `httpx` (multithreaded file downloads), SQLite session persistence.

## Status (as of 2026-06-11)
- Branch: `main`
- Recent work: real YouTube title via oEmbed + instant quality dialog + correct output naming, fixed YouTube URL stream detection / SABR download errors, added Firefox-specific extension folder, Linux Mint compatibility + browser extension improvements.

## Active Work / Next Steps
- No open work items recorded beyond the recent fixes above.

## Notes
- Personal fork: `Lovepankie/PIDM` (local directory `PIDM-main`)
